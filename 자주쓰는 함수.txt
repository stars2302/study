1. 숫자
Math.round(수);
Math.ceil(수);
Math.floor(수);
Math.trunc(수);
Math.abs(수);
Math.random();
Math.sing(수);

2. 문자
문자.length;
Array.from(문자).length
문자.trim()
문자.indexOf()  /  문자.lastIndexOf()
문자.includes()
문자.charAt()
문자.slice()
문자.substring()
문자.substr()
문자.replace()
문자.split()
문자.toLowerCase()  /  문자.toUpperCase()

3. 배열